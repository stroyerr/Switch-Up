using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class introLevel : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // themeController.setTheme(4);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
